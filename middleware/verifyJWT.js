const jwt = require('jsonwebtoken');
require('dotenv').config();

// const verifyJWT = (req,res,next) => {
//   const authHeader = req.headers['authorization'];
//   console.log("authHeader", authHeader, "objects": req.headers);
//   if(!authHeader)

// }